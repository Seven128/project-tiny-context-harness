core:
  package: "agent-project-sdlc"
  version: "0.1.0"
  schema_version: "1"

managed_files:
  - path: "AGENTS.md"
    strategy: "merge-block"
  - path: "Makefile"
    strategy: "merge-block"
  - path: ".agent/prompts"
    strategy: "managed"
  - path: ".agent/pjsdlc_managed/templates"
    strategy: "managed"
  - path: ".agent/pjsdlc_managed/policies"
    strategy: "merge-with-local"
  - path: ".agent/pjsdlc_managed/make/sdlc-harness.mk"
    strategy: "managed"
  - path: ".github/workflows/harness.yml"
    strategy: "create-if-missing"

local_overrides:
  - ".agent/overrides/**"
  - ".agent/pjsdlc_managed/policies/*.local.yaml"

never_overwrite:
  - ".docs/**"
  - ".agent/state/**"
  - "src/**"
  - "tests/**"
