{
  "id": "verification-deployment-health",
  "title": "Make degraded health fail closed",
  "category": "verification_deployment",
  "prompt": "Strengthen the health contract. `healthStatus()` must expose component-level billing, notifications, and worker checks and compute `ok` versus `degraded`. Running `node src/health.mjs --worker-down` must print the degraded JSON result and exit non-zero. Add tests, keep the ready smoke passing, and update the owning verification and deployment Context.",
  "paths": [
    "src/health.mjs",
    "tests/base.test.mjs",
    "package.json"
  ],
  "terms": [
    "health",
    "readiness",
    "degraded",
    "exit code",
    "verification",
    "deployment"
  ],
  "facets": [
    "verification",
    "deployment"
  ],
  "expected_context_delta": "required",
  "probe": "verification-deployment-health.mjs",
  "track_family": "context-workflow"
}
