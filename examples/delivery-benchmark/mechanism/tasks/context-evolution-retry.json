{
  "id": "context-evolution-retry",
  "title": "Finish bounded retry and dead-letter lifecycle",
  "category": "context_evolution",
  "prompt": "Fix retry exhaustion. A failed job below `maxAttempts` is requeued as `retrying`; the failure that reaches `maxAttempts` becomes `dead_lettered`, is not requeued, and appears exactly once in `status().deadLetters`. Add regression coverage and update the retry lifecycle Context if implementation work reveals or changes the durable exhaustion rule.",
  "paths": [
    "src/worker/retryWorker.mjs",
    "tests/base.test.mjs"
  ],
  "terms": [
    "retry",
    "maxAttempts",
    "dead letter",
    "DLQ",
    "recovery"
  ],
  "facets": [
    "contract",
    "verification"
  ],
  "expected_context_delta": "required",
  "probe": "context-evolution-retry.mjs",
  "track_family": "context-workflow"
}
