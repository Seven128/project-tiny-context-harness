# Recovery Checkpoint

Pause after the RFC change has been introduced but before final testing.

A fresh Agent should be able to answer:

- Whether the implementation accepts both `jurisdiction` and deprecated `region`.
- Whether audit output has been updated from region terminology to jurisdiction terminology.
- Which acceptance criteria still need evidence.
- Which tests or CLI smoke commands must run next.
- Whether any compatibility risk remains.
