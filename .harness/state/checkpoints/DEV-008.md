# Checkpoint: DEV-008

## 1. 当前状态

- Phase: `SPRINTING`
- Task ID: `DEV-008`
- Status: `in_progress`
- Active skill: `dev_sprint`

## 2. 已读取事实源

- `.harness/state/lifecycle.yaml`
- `.harness/state/tasks.yaml`
- `.harness/skills/dev_sprint/SKILL.md`
- `.docs/rfc/RFC_002_configurable_harness_root.md`
- `.docs/01_product/npm_package_distribution.md`
- `.docs/02_architecture/harness_package_distribution.md`
- `.docs/03_tech_plan/harness_package_distribution.md`

## 3. 已完成工作

- 进入 RFC 并写入 `RFC_002_configurable_harness_root.md`。
- 将当前仓库配置为 `package.json#sdlcHarness.harnessFolderName = ".harness"`。
- 将 Skill canonical source 从 `.harness/agents/skills/**` 移动到 `.harness/skills/**`。
- 新增 Node 侧 Harness root 读取逻辑，支持 `package.json`、`sdlc-harness.config.json` 和 `harnessFloderName` 兼容别名。
- 开始把 `init`、`sync`、`doctor`、`migrations`、`validators` 迁移到动态 `<harnessRoot>`。

## 4. 当前改动文件

- `package.json`
- `AGENTS.md`
- `README.md`
- `.harness/config.yaml`
- `.harness/skills/**`
- `.harness/state/lifecycle.yaml`
- `.harness/state/tasks.yaml`
- `packages/sdlc-harness/**`
- `tests/sdlc-harness/**`
- `tools/validate_harness.py`
- `tools/validate_prompt_language.py`
- `.docs/01_product/npm_package_distribution.md`
- `.docs/02_architecture/harness_package_distribution.md`
- `.docs/03_tech_plan/harness_package_distribution.md`
- `.docs/rfc/RFC_002_configurable_harness_root.md`

## 5. 未完成工作

- 补齐 README 中的 `<harnessRoot>` 说明。
- 更新 `.harness/config.yaml`、产品文档和测试中的旧 `.harness/agents/skills` 引用。
- 补充默认 `.agents`、显式 `.harness`、`sdlc-harness.config.json`、`harnessFloderName` 的测试覆盖。
- 同步包内 assets，运行 DEV-008 required gates。
- 写入 implementation doc，刷新 docs overview，并标记 DEV-008 done。

## 6. Gate 状态

- `make validate-rfc`: PASS
- `npm test`: pending
- `node packages/sdlc-harness/dist/cli.js package check-source`: pending
- `node packages/sdlc-harness/dist/cli.js validate-harness`: pending
- `make validate-harness`: pending

## 7. 风险和阻塞

- 无 blocker。
- 需要确保默认项目 materialize 到 `.agents/**`，当前仓库显式 materialize 到 `.harness/**`。

## 8. 下一步

- 完成文档、配置和测试迁移，构建包后运行 required gates。
