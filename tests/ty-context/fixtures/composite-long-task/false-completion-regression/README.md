# HFC-003 False-Completion Regression Fixtures

Committed mini workdirs for known false-completion scenarios. The runner copies each fixture into a temporary `tmp/ty-context/plan-acceptance/demo` workdir and verifies the existing Trusted Evidence Kernel, validator, derived output scan and completion-output resolver.
